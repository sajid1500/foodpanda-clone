import { getLocationName, getUser } from "@/lib/data-service";

export default async function AddressPicker() {
  // const locationName = await getLocationName(lat, lng);
  return <div>north basabo</div>;
}
